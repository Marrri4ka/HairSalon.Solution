Stylist <strong>@ViewBag.Stylist.GetName()</strong> has these clients:
@foreach (var client in ViewBag.Clients1) {
<li>@client.GetName()</li> <br />
}


<form action="/stylists/@ViewBag.Stylist.GetId()/addclient" method="post">
  <select name="clientId" id="clientId">
    @foreach (var client in ViewBag.Clients) {
    <option value="@client.GetId()">@client.GetName() </option>
    }
  </select>
  <button type="submit">Select</button>
</form>


Stylist <strong>@ViewBag.Stylist.GetName()</strong> has these specialities:
@foreach (var speciality in ViewBag.Specialities1) {
<li>@speciality.GetName()</li> <br />
}


<form action="/stylists/@ViewBag.Stylist.GetId()/addspeciality" method="post">
  <select name="specialityId" id="specialityId">
    @foreach (var speciality in ViewBag.Specialities) {
    <option value="@speciality.GetId()">@speciality.GetName()</option>
    }
  </select>
  <button type="submit">Add</button>
</form>